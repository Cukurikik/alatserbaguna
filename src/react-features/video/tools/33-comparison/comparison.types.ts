export interface ComparisonOptions {
  splitMode: 'vertical' | 'horizontal' | 'overlay';
  splitPosition: number;
  syncPlayback: boolean;
  labelA: string;
  labelB: string;
  exportAsSideBySide: boolean;
}
